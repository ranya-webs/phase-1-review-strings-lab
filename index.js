// Define a string variable, currentUser.
const currentUser = "Ranya";

//Write a welcome message to the current user of a social network called Flatbook 
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`;

//Write an excited welcome message -> outputs a sentence with all letters being uppercase
const excitedWelcomeMessage = welcomeMessage.toUpperCase();

//Write a short greeting containing the first letter of the currentUser name
const shortGreeting = `Welcome, ${currentUser[0]}!`;